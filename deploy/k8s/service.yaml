apiVersion: v1
kind: Service
metadata:
  name: lurus-www
  namespace: lurus-www
  labels:
    app: lurus-www
    app.kubernetes.io/name: lurus-www
    app.kubernetes.io/part-of: lurus
spec:
  selector:
    app: lurus-www
    app.kubernetes.io/name: lurus-www
    app.kubernetes.io/part-of: lurus
  ports:
    - name: http
      port: 3000
      targetPort: http
